### UI구현과 화면구현
1. 폴더관리하는 방법
  - 기본적인 정보 테스트하기
  - 브라우저를 통해서 테스트(html, css, js)를 할 때는 live server 사용
  - 만일 js만 단독으로 테스트 할 때는 node 이름.js엔터하기(출력결과는 터미널에 나감)
  -js만 집중적으로 확인하고자 할 때는 매번 node 이름.js하는 것은 불편 <- nodemon
2. npm init으로 폴더 관리하는 방법
3. parcel번들러 활요해서 폴더 관리하는 방법